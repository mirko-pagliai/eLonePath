# ===============================================================================
# Environment Configuration File
# ===============================================================================
#
# This file serves as a template for environment-specific configuration.
# Copy this file to `.env` in the project root to configure your environment.
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp config/.env.example config/.env
# 2. Edit the .env file with your specific values
# 3. The .env file is automatically loaded by `config/bootstrap.php` (if it exists)
# 4. Never commit the .env file to version control (it's in `.gitignore`)
#
# ===============================================================================

# -------------------------------------------------------------------------------
# DEBUG MODE
# -------------------------------------------------------------------------------
#
# Controls application debug behavior and error visibility.
#
# When DEBUG=true:
#   - Detailed error messages are displayed in the browser
#   - Exception stack traces are shown in error pages
#   - Full error details are logged to console (error_log + STDERR)
#   - Intended for DEVELOPMENT environments only
#
# When DEBUG=false:
#   - Generic error messages are displayed in the browser
#   - Exception details are hidden from users
#   - Errors are still logged to console for administrators
#   - Intended for PRODUCTION environments
#
# ACCEPTED VALUES: true, false, 1, 0, yes, no, on, off
# DEFAULT (if not set or .env doesn't exist): false
#
DEBUG=false
