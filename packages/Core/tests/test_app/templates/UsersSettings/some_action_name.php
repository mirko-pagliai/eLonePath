<?php
declare(strict_types=1);

/**
 * Minimal, generic fixture — proves `Dispatcher::templateName()` snake_cases a multi-word action name
 * (`someActionName()`) when resolving the template path, unlike the controller portion (`UsersSettings`),
 * which is used as-is.
 */
?>

Some action name page.
