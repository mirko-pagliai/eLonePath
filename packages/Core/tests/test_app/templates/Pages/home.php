<?php
declare(strict_types=1);

/**
 * Minimal, generic fixture — used to verify that `Dispatcher::dispatch()` and `View::render()` actually render a
 * real template file end to end, not just compute its name or its data.
 */
?>

Home page.
